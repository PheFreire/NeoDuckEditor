-- Km.set("n", ":", function()
--   vim.ui.input({
--     prompt = "CmdLine",
--     completion = "command",
--   }, function(input)
--     if not input then return end

--     vim.cmd(input)
--   end)
-- end, {desc = ": cmdline replacement"})
